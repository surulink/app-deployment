apiVersion: v1
kind: Service
metadata:
  name: example-service
  labels:
    app: hello-app
spec:
  type: LoadBalancer
  selector:
    app: hello-app
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 5000
